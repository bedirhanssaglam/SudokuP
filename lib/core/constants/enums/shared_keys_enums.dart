enum SharedKeysEnums {
  name("name"),
  isLogged("isLogged"),
  isFirst("isFirst"),
  languageCode("language_code"),
  currentDifficultyLevel("currentDifficultyLevel"),
  currentTheme("currentTheme"),
  currentAccentColor("currentAccentColor");

  final String key;
  const SharedKeysEnums(this.key);
}
